name: Build Android

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK/AAB
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # TODO: implement full Android build steps
